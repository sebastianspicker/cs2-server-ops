# From `cs2-auto-update`

The updater now lives at `apps/maintain/updater`.

What changed:

- the script and tests stay script-first
- shared docs and examples now live at repo root
- you can still use the updater without deploying the panel
