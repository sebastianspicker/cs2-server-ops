# Remediation ledger

Source of truth: `docs/refactor-plan.md`.

Status values: `NOT_STARTED`, `IN_PROGRESS`, `BLOCKED`, `DEFERRED`,
`IMPLEMENTED`, `VERIFIED`, `COMPLETE`.

| Slice ID | Title | Priority | Findings | Status | Files changed | Tests | Verification | Result | Blocker/defer reason | Uncertainty | Last note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RP-001 | Re-establish a trusted verification baseline | Verification blocker | verification baseline blockers | COMPLETE | `docs/verification-baseline.md` | None added | `docker info`; `npx -y node@22 --version`; `npx -y -p node@22 -p npm@10 npm --version`; panel `format:check`, `lint`, `typecheck`, `build`, `test`, `test:e2e`, `validate`, `validate -- --require-docker`; updater `make ci`; root `./scripts/verify.sh` | Node 22 panel non-Docker checks passed; updater CI passed; Docker-required checks/root verifier blocked by unavailable daemon and documented | None | No live CS2/RCON runtime exercised | Baseline refreshed with exact pass/fail/skipped state on 2026-05-26. |
| RP-002 | Remove deprecated Node test-mocking API usage | Verification blocker | DSA-009 | COMPLETE | `apps/operate/panel/test/mock-module.ts`; panel mock-using tests | Updated mock setup only; no behavior assertions removed | `rg -n "defaultExport\|namedExports" apps/operate/panel/test apps/operate/panel/types`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm test`; `node --experimental-test-module-mocks --test --test-timeout 120000 dist/test/game-helpers.test.js` | No deprecated option source hits; typecheck passed; Node 22 unit suite passed `240` tests; Node 26 targeted helper suite passed without mock option deprecation warnings | None | Helper keeps Node 22 compatibility because Node 22 does not support `options.exports`; Node 26 uses `options.exports` | Completed after adding a test-only compatibility helper. |
| RP-003 | Make RCON add and reconnect fail truthfully | High | LCA-001 | COMPLETE | `apps/operate/panel/modules/rcon.ts`; `apps/operate/panel/routes/server.ts`; `apps/operate/panel/test/rcon-manager.test.ts`; `apps/operate/panel/test/server-crud.test.ts`; `apps/operate/panel/docs/API.md` | Added manager failure propagation test and add/reconnect route regressions for post-probe connection failure | `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run build` | Typecheck passed; unit suite passed `243` tests; lint passed; build passed | None | No live unreachable RCON endpoint was available for manual smoke; behavior is covered with fakes/mocked routes | `connectServer()` now returns connection proof and add/reconnect routes return `502` instead of optimistic success when no authenticated connection exists. |
| RP-004 | Revalidate session users and admin state | High | LCA-002 | COMPLETE | `apps/operate/panel/modules/middleware.ts`; `apps/operate/panel/routes/users.ts`; `apps/operate/panel/test/user-management.test.ts`; `apps/operate/panel/docs/API.md` | Added deleted-user stale-session rejection and admin-demotion revalidation regressions | `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run build` | Typecheck passed; unit suite passed `245` tests; lint passed; build passed | None | No distributed session store or browser manual smoke was exercised; DB-backed request tests cover the stale user and stale admin contracts | Protected requests now revalidate the session user against SQLite and admin routes use the current stored admin flag. |
| RP-005 | Represent partial RCON status honestly | Medium | LCA-003 | COMPLETE | `apps/operate/panel/routes/status.ts`; `apps/operate/panel/public/ts/manage.ts`; `apps/operate/panel/public/ts/servers.ts`; `apps/operate/panel/test/status.test.ts`; `apps/operate/panel/test/e2e/panel.spec.ts`; `apps/operate/panel/docs/API.md` | Added API regressions for partial RCON observations and full RCON failure; added Playwright checks for partial manage-page rendering and server-list unknown player counts | `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run test:e2e`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run build` | Typecheck passed; unit suite passed `252` tests; Playwright passed `7` tests; format check passed; lint passed; build passed | None | No live CS2/RCON runtime was available; connection/auth state is covered with mocked RCON manager connection info and browser route fixtures | Status JSON now reports `partial`/`complete`, derives connection/authentication from RCON connection info, and UI displays unknown player counts as unknown rather than `0`. |
| RP-006 | Report multi-command RCON partial failures explicitly | High | LCA-009 | COMPLETE | `apps/operate/panel/routes/game/helpers.ts`; `apps/operate/panel/routes/game/controls.ts`; `apps/operate/panel/test/game-routes.test.ts`; `apps/operate/panel/docs/API.md` | Added multi-command route regressions for second-command failures across respawn, startmoney, roundtime, overtime, and a sequence route | Pre-fix `npx -y -p node@22 -p npm@10 npm test` failed with 4 expected partial-response assertions; post-fix `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run build` | Format check passed; typecheck passed; unit suite passed `246` tests; lint passed; build passed | None | No live CS2/RCON runtime smoke was available; behavior is covered with mocked RCON route tests | Multi-command controls now execute in order and return explicit `partial`, `applied_commands`, `failed_command`, and `failed_command_index` details when later commands fail. |
| RP-007 | Make numeric preset parsing strict | Medium | LCA-004 | COMPLETE | `apps/operate/panel/routes/game/helpers.ts`; `apps/operate/panel/test/game-helpers.test.ts`; `apps/operate/panel/test/game-routes.test.ts`; `apps/operate/panel/docs/API.md` | Added helper boundary tests for suffix, prefix, decimal, empty, zero, allowed integer, and invalid inputs; added route tests proving malformed preset inputs return `400` before any RCON command is sent | Pre-fix `npx -y -p node@22 -p npm@10 npm test` failed with `5` expected strict-parsing regressions; post-fix `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run build` | Unit suite passed `261` tests; typecheck passed; format check passed; lint passed; build passed | None | No live CS2/RCON runtime was available; mocked route tests prove malformed inputs do not reach RCON command execution | `parseIntBody()` now accepts only safe integers or whole-number strings, rejecting trailing junk, prefixes, decimals, empty input, unsafe numbers, and non-numeric values. |
| RP-008 | Align player userid parsing and action validation | Medium | LCA-005 | COMPLETE | `apps/operate/panel/utils/rconParsers.ts`; `apps/operate/panel/routes/game/match.ts`; `apps/operate/panel/test/rcon-parsers.test.ts`; `apps/operate/panel/test/game-routes.test.ts`; `apps/operate/panel/docs/API.md` | Added parser boundary coverage for five-digit and six-digit userids; added route coverage proving five-digit `player-kick` is accepted and six-digit input is rejected before RCON | Pre-fix `npx -y -p node@22 -p npm@10 npm test` failed with the expected five-digit route mismatch plus temporary login-limit artifacts from the initial test shape; targeted pre-fix `npx -y node@22 --experimental-test-module-mocks --test --test-timeout 120000 dist/test/rcon-parsers.test.js dist/test/game-routes.test.js` failed on the five-digit route boundary; post-fix `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run build` | Typecheck passed; unit suite passed `262` tests; format check passed; lint passed; build passed | None | No live CS2/RCON runtime was available; mocked parser and route tests cover the command boundary | Parser and action routes now share `RCON_USERID_RE` for the documented `1`- to `5`-digit userid contract. |
| RP-009 | Parse latest-backup cvar output correctly | Medium | LCA-008 | COMPLETE | `apps/operate/panel/routes/game/match.ts`; `apps/operate/panel/test/game-routes.test.ts` | Added route coverage for quoted cvar output, unquoted `key=value`, empty output, malformed output, and unsafe filenames | Pre-fix `npx -y -p node@22 -p npm@10 npm test` failed with the expected quoted-cvar restore-latest regression; targeted pre-fix `npx -y node@22 --experimental-test-module-mocks --test --test-timeout 120000 --test-name-pattern "command-boundary" dist/test/game-routes.test.js` failed on the quoted latest-backup case; post-fix `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run build` | Typecheck passed; unit suite passed `262` tests; format check passed after formatter rewrite; lint passed; build passed | None | No live CS2/RCON runtime was available; mocked route tests cover cvar parsing and restore command dispatch | `restore-latest-backup` now extracts quoted or unquoted `mp_backup_round_file_last` values before applying existing filename sanitization. |
| RP-010 | Normalize default admin bootstrap username | Medium | LCA-011 | COMPLETE | `apps/operate/panel/db.ts`; `apps/operate/panel/test/entrypoint.test.ts` | Added startup coverage for normal usernames, leading/trailing whitespace normalization, and whitespace-only username rejection | Pre-fix `npx -y -p node@22 -p npm@10 npm test` failed with two expected bootstrap regressions: padded default username could not be found by normalized login, and whitespace-only username started successfully; post-fix `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run build` | Typecheck passed; unit suite passed `264` tests; format check passed; lint passed; build passed | None | No migration was added for existing bad bootstrap rows; this slice only prevents new unreachable default admins | Default admin bootstrap now trims `DEFAULT_USERNAME`, rejects empty or overlong normalized names, and stores the same username that login normalization will query. |
| RP-011 | Restore saved manage-page game selection | Medium | LCA-006 | COMPLETE | `apps/operate/panel/routes/server.ts`; `apps/operate/panel/views/manage.ejs`; `apps/operate/panel/public/ts/manage.ts`; `apps/operate/panel/test/e2e/panel.spec.ts` | Added Playwright coverage for restoring a valid saved game type/mode/map and deliberately falling back for invalid saved state | Pre-fix `npx -y -p node@22 -p npm@10 npm run test:e2e` failed with the expected manage-page regression: `#gameTypeValue` expected `fun` but received `competitive`; post-fix `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run test:e2e`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm test` | Typecheck passed; Playwright passed `8` tests; format check passed after formatter rewrite; lint passed; unit suite passed `264` tests; `test:e2e` rebuilt the panel successfully | None | No live CS2/RCON runtime was available; Playwright covers persisted SQLite state, rendered hidden inputs, active buttons, map select value, and invalid-state fallback in Chromium | Manage page now validates saved selections against `mapsConfig`, renders validated initial values, and the client initializer restores those values instead of always choosing the first game type/mode/map. |
| RP-012 | Correct autocomplete cache-hit reporting | Low | LCA-007 | COMPLETE | `apps/operate/panel/routes/operator.ts`; `apps/operate/panel/test/game-routes.test.ts` | Added autocomplete route coverage for first fetch `cached: false`, second fetch within TTL `cached: true`, and explicit refresh `cached: false` | Pre-fix `npx -y node@22 --experimental-test-module-mocks --test --test-timeout 120000 --test-name-pattern autocomplete dist/test/game-routes.test.js` failed because the first fresh autocomplete request returned `cached: true`; post-fix targeted autocomplete test passed; an initial separate-test shape added one login and tripped existing file-level login-limit pressure in the broad suite, then the assertion was folded into the existing autocomplete test; post-fix `npx -y -p node@22 -p npm@10 npm run build`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm test` | Build passed; targeted autocomplete test passed; format check passed; typecheck passed; lint passed; unit suite passed `277` tests | None | Autocomplete behavior was verified with mocked RCON route tests, not live RCON `cmdlist`/`cvarlist` output from a CS2 server | `loadAutocomplete()` now reports whether an unexpired cache entry was actually reused instead of deriving the response flag after storing a fresh entry. |
| RP-013 | Add CFG integrity coverage before changing modes | Medium | LCA-010, DSA-003, DSA-004, DSA-005 | COMPLETE | `apps/operate/panel/cfg/oitc.cfg`; `apps/operate/panel/cfg/1v1arenas.cfg`; `apps/operate/panel/test/cfg-integrity.test.ts`; `apps/operate/panel/test/game-routes.test.ts`; `apps/operate/panel/docs/SERVER-SETUP.md` | Added CFG integrity tests for every `maps.json` mode exec target, hard-coded route CFG targets including server-provided `live.cfg`, map-group references, and a setup-game route assertion that runtime CFG exec failure stops before map change | Pre-fix `npx -y -p node@22 -p npm@10 npm test` failed with the expected missing CFG integrity result for `fun/oitc: oitc.cfg` and `fun/1v1arenas: 1v1arenas.cfg`; the initial standalone route assertion also triggered existing login-limit pressure before being folded into an already authenticated flow; post-fix `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run build` | Unit suite passed `267` tests; typecheck passed; format check passed; lint passed; build passed | Live RCON `exec` smoke was not available because no CS2/RCON runtime is available in this environment | OITC and 1v1 arena CFGs are conservative base-rule configs; plugin/map-script behavior still depends on the operator's server runtime | Missing mode CFGs are now repo-managed, `live.cfg` is explicitly documented as server-provided from `cfg/server-provided/live.cfg`, and tests fail if future map-mode exec targets lack a CFG file. |
| RP-014 | Make migration compatibility fixture-backed | High | LCA-013, DSA-008 | COMPLETE | `apps/operate/panel/db.ts`; `apps/operate/panel/test/migrations.test.ts` | Added fixture-backed SQLite migration tests for fresh v0, supported v1, duplicate `users.is_admin`, current v3, and malformed unsupported v1 schemas | Pre-fix `npx -y -p node@22 -p npm@10 npm test` failed with duplicate-column and unsupported-schema fixture failures; post-fix `npx -y node@22 --test --test-timeout 120000 dist/test/migrations.test.js`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run build` | Migration fixture test passed `5` tests; format check passed; typecheck passed; unit suite passed `251` tests; lint passed; build passed | None | No real operator backup DB was available for manual smoke; fixture child-process imports cover the defined compatibility shapes | Migrations now validate known schema versions, reject malformed historical DBs clearly, and skip duplicate admin-column creation while preserving existing admin flags. |
| RP-015 | Make RCON authentication timeout deterministic | Medium | LCA-014 | COMPLETE | `apps/operate/panel/modules/rcon.ts`; `apps/operate/panel/test/rcon-manager.test.ts` | Added a fake RCON client whose `authenticate()` never resolves and assertions that both `connectServer()` and `probeServer()` settle through the auth timeout | Pre-fix `npx -y node@22 --experimental-test-module-mocks --test --test-timeout 120000 dist/test/rcon-manager.test.js` failed with `connectServer did not settle within auth timeout`; post-fix `npx -y node@22 --experimental-test-module-mocks --test --test-timeout 120000 dist/test/rcon-manager.test.js`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run build` | Targeted RCON manager test passed `4` tests; full unit suite passed `268` tests; typecheck passed; format check passed after formatter rewrite; lint passed; build passed | Optional network blackhole/manual timeout smoke was not run because no live RCON/network-smoke target is available | Timeout value is now env-configurable for tests via `RCON_AUTH_TIMEOUT_MS`; production default remains `10000` ms | RCON authentication now races the dependency authenticate promise against an explicit timeout rejection, so socket destruction is cleanup rather than the only settlement mechanism. |
| RP-016 | Isolate or justify the E2E seed route | Medium | DSA-011 | COMPLETE | `apps/operate/panel/app.ts`; `apps/operate/panel/playwright.config.ts`; `apps/operate/panel/test/app.test.ts`; `apps/operate/panel/test/e2e-seed-route-boundary.test.ts`; `apps/operate/panel/test/e2e/panel.spec.ts` | Added app-level regression that the legacy seed route is not mounted even if the old route flag is present; added production-mode route absence coverage with Redis mocked; moved Playwright seeding to direct E2E SQLite setup | Pre-fix `npx -y node@22 --experimental-test-module-mocks --test --test-timeout 120000 dist/test/app.test.js` failed because `/api/test/servers` returned `401` instead of `404`; post-fix targeted boundary tests passed; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run test:e2e` | Typecheck passed; format check passed after formatter rewrite; lint passed; unit suite passed `270` tests; Playwright passed `8` tests | None | Production-mode route absence test mocks Redis so the production app can be imported without a real Redis server; no Docker image runtime smoke was available | The HTTP-only E2E seed route was removed from the app; Playwright now seeds through `E2E_DB_PATH`, keeping seed writes inside the E2E harness instead of the production route surface. |
| RP-017 | Replace low-signal source-text tests with behavior coverage | Low | LCA-012 | COMPLETE | `apps/operate/panel/test/e2e/panel.spec.ts`; `apps/operate/panel/test/scripts.test.ts` | Added Playwright coverage for Enter-key login submission, Enter-key add-server submission, advanced manage controls staying collapsed until opened, and admin usernames rendering as text rather than injected markup; removed low-signal template source regexes and narrowed the remaining admin-users source check to an `innerHTML` assignment invariant | `npx -y -p node@22 -p npm@10 npm run format:check`; initial `npx -y -p node@22 -p npm@10 npm run typecheck` failed on Playwright callback `window` globals; `npx -y -p node@22 -p npm@10 npm run lint`; post-fix `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run test:e2e` | Format check passed; typecheck passed after switching browser callbacks to `globalThis`; lint passed; unit suite passed `275` tests; Playwright passed `10` tests | None | Browser behavior coverage runs in Chromium with seeded SQLite state, not across all browsers or a live CS2/RCON server | Source-text coverage now stays limited to a specific raw-HTML assignment invariant; login, add-server, manage advanced sections, and admin username rendering are covered through browser behavior. |
| RP-018 | Deduplicate security-sensitive RCON display sanitizers | Medium | DSA-014 | COMPLETE | `apps/operate/panel/utils/rconDisplay.ts`; `apps/operate/panel/utils/rconResponse.ts`; `apps/operate/panel/utils/rconParsers.ts`; `apps/operate/panel/public/ts/manage.ts`; `apps/operate/panel/test/rcon-display.test.ts`; `apps/operate/panel/test/rcon-response.test.ts`; `apps/operate/panel/test/rcon-parsers.test.ts`; `apps/operate/panel/test/e2e/panel.spec.ts` | Added shared sanitizer fixture tests for HTML special characters, already-escaped text, control/zero-width/bidirectional characters, trimming, truncation, hostname parsing, player display names, and browser console rendering | `npx -y -p node@22 -p npm@10 npm run build`; `npx -y node@22 --experimental-test-module-mocks --test --test-timeout 120000 dist/test/rcon-display.test.js dist/test/rcon-response.test.js dist/test/rcon-parsers.test.js`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run test:e2e` | Build passed; focused sanitizer/parser suite passed `24` tests; typecheck passed; format check passed after formatter rewrite; lint passed; unit suite passed `277` tests; first Playwright run failed because the new test targeted a hidden collapsed RCON input, then passed `9` tests after opening the console section | None | Browser console display was verified with a Playwright route fixture, not live RCON output from a CS2 server | Hostname/status/player parsing and browser RCON console display now share `cleanRconDisplayText()` for unsafe display-control removal; HTML escaping behavior remains DOM/textContent based rather than string-escaping in the sanitizer. |
| RP-019 | Delete only proven-unused browser and type exports | Low | DSA-001, DSA-002 | COMPLETE | `apps/operate/panel/public/ts/common.ts`; `apps/operate/panel/utils/mapsConfig.ts`; `docs/code-index.md` | None added; deletion was proven with deterministic reference checks | Pre-delete `rg "escapeHtml\|MapConfig\|GameTypeConfig\|GameModeConfig" apps docs`; pre-delete `rg -n "from '../utils/mapsConfig\|mapsConfig\|getMapsForMode\|GameMode\|GameType\|MapGroup\|MapsConfig" apps/operate/panel/routes apps/operate/panel/test apps/operate/panel/utils apps/operate/panel/public/ts -g '!dist/**'`; post-delete `rg "escapeHtml\|MapConfig\|GameTypeConfig\|GameModeConfig" apps docs`; post-delete `rg -n "export type (GameMode\|GameType\|MapGroup\|MapsConfig)\|export function escapeHtml\|\bescapeHtml\b" apps/operate/panel -g '!dist/**'`; `npx -y -p node@22 -p npm@10 npm run build:client`; `npx -y -p node@22 -p npm@10 npm run format:check`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run test:e2e` | App export check returned no active matches for `escapeHtml` or exported map config type aliases; exact plan `rg` now only finds historical audit/plan docs; build:client passed; format check passed; typecheck passed; lint passed; unit suite passed `275` tests; Playwright passed `10` tests | None | Out-of-tree TypeScript or browser imports cannot be proven absent from this repository alone | Removed unused `escapeHtml` and exported map config type aliases; active map config API remains `mapsConfig` and `getMapsForMode`. |
| RP-020 | Resolve exact compatibility helpers only with usage evidence | Medium | DSA-006, DSA-007, DSA-016 | COMPLETE | `apps/maintain/updater/CONTRIBUTING.md` | None added; existing updater warning tests retained | `rg "validate.sh\|NOTIFY_WEBHOOK_URL\|NOTIFY_PLAYERS_MESSAGE\|RCON_CLI\|RCON_HOST\|RCON_PORT\|RCON_PASSWORD\|ci-install-tools\|ci-tools-versions" .github apps configs docs scripts`; `git log --all --oneline --follow -- scripts/validate.sh`; `git log --all --oneline --follow -- apps/maintain/updater/scripts/ci-install-tools.sh`; `git log --all --oneline --follow -- apps/maintain/updater/scripts/ci-tools-versions.env`; `git log --all --oneline -G "NOTIFY_WEBHOOK_URL\|REMOVED_CONFIG_VARS\|RCON_CLI\|RCON_PASSWORD" -- apps/maintain/updater/update_cs2.sh apps/maintain/updater/tests/run.sh apps/maintain/updater/CHANGELOG.md`; `make ci`; `./scripts/verify.sh` | Reference and history checks showed active repo/docs/test usage for all candidates; updater `make ci` passed `49` tests including removed-key warnings; root verifier passed shared shell/config checks then failed at Docker daemon access in the operate-module Docker fallback | No deletion: root `validate.sh` remains an active compatibility command; removed updater config-key warnings are documented in changelog and tested; CI tool installer remains a manual contributor helper | No external operator telemetry was available; root verifier remains Docker-blocked in this environment | Kept compatibility paths with evidence instead of deleting them; clarified updater contributor docs that `ci-install-tools.sh` is for local/manual setup while GitHub Actions installs tools through `apt`. |

## Source-plan deferred findings

| ID | Status | Reason |
| --- | --- | --- |
| DSA-010 | DEFERRED | Module-system migration is broad; defer until panel tests and E2E remain stable. |
| DSA-012 | DEFERRED | Duplicate toast/POST handling should wait for behavior coverage and enough call sites to justify abstraction. |
| DSA-013 | DEFERRED | Duplicate `parsePort` is not actionable without a third use or real bug. |
| DSA-015 | DEFERRED | Broad file splitting is not a standalone cleanup target. |
| DSA-017 | DEFERRED | Duplicate RCON history response type is deferred until type drift or nearby slice evidence appears. |
