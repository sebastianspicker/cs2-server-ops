# From `cs2-modded-server-egg`

The archived egg is no longer a canonical runtime path.

What survived:

- startup/env conventions worth keeping
- plugin/admin bootstrap patterns
- migration guidance for operators leaving egg-based deployments

What did not:

- a Pterodactyl-first deployment model
- archived packaging as the default shipping artifact
