# From `cs2-modded-server-panel`

The panel now lives at `apps/operate/panel`.

What changed:

- the panel is one module inside a larger lifecycle repo
- repo-level docs now describe provisioning and maintenance alongside the web UI
- Pterodactyl-specific delivery material is no longer part of the primary operator story
