# Simplicity Remediation Ledger

Source plan: `docs/simplicity-remediation-plan.md`

Status values: `NOT_STARTED`, `IN_PROGRESS`, `BLOCKED`, `DEFERRED`,
`IMPLEMENTED`, `VERIFIED`, `COMPLETE`.

| Slice ID | Title | Severity | Findings | Status | Files changed | Tests | Verification | Result | Blocker/defer reason | Uncertainty | Last note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SRP-001 | Make RCON init and health readiness explicit | P1 | `STC-FL-001`| COMPLETE |`apps/operate/panel/modules/rcon.ts`; `apps/operate/panel/app.ts`; `apps/operate/panel/test/rcon-manager.test.ts`; `apps/operate/panel/test/app.test.ts`; `apps/operate/panel/test/e2e/panel.spec.ts`; `apps/operate/panel/docs/API.md`| Added init-summary unit coverage, health readiness API coverage, and E2E readiness assertion. |`npm run typecheck`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run test:e2e` | Complete: health now exposes RCON readiness separately from liveness and records saved-server init failure counts/errors. | - | Local default Node could not run native SQLite tests due ABI mismatch; Node 22 verification matches project requirement. Worktree remains dirty from prior unrelated changes. | 2026-05-26: Typecheck passed; unit suite passed 278 tests; E2E passed 10 tests.  &#124; |
| SRP-002 | Add explicit unknown/timed-out server status API states | P1 | `STC-FL-002`, `STC-TI-003`| COMPLETE |`apps/operate/panel/routes/server.ts`; `apps/operate/panel/test/server-crud.test.ts`; `apps/operate/panel/docs/API.md`| Replaced weak server-list assertion with access-scope, observed, timed-out, and failed-probe API tests. |`npm run typecheck`; `npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern servers`; `npx -y -p node@22 -p npm@10 npm run test:e2e`| Complete:`/api/servers`now exposes canonical`status`, `observed_at`, `status_source`, `timed_out`, and `error` fields while preserving legacy booleans as compatibility fields. | - | The npm test-name pattern is appended after the script's file glob, so the command ran the full suite instead of only server tests. | 2026-05-26: Typecheck passed; unit suite passed 281 tests; E2E passed 10 tests.  &#124; |
| SRP-003 | Render dashboard/manage status uncertainty | P1 | `STC-FL-002`, `STC-FL-009`, `STC-TI-003`| COMPLETE |`apps/operate/panel/routes/server.ts`; `apps/operate/panel/views/manage.ejs`; `apps/operate/panel/public/ts/servers.ts`; `apps/operate/panel/public/css/panel.css`; `apps/operate/panel/test/e2e/panel.spec.ts`| Added Playwright coverage for initial manage status uncertainty, unknown/timed-out server-list cards, and secondary status-fetch failure. |`npm run build:client`; `npx -y -p node@22 -p npm@10 npm run test:e2e`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run lint` | Complete: dashboard cards and manage header now show unknown/timed-out/unavailable states instead of rendering unobserved status as definitively disconnected. | - | No live CS2/RCON runtime was available; behavior is covered with route mocks and seeded E2E state. | 2026-05-26: Client build passed; E2E passed 12 tests; unit suite passed 281 tests; lint passed.  &#124; |
| SRP-004 | Prove add-server persistence, access, encryption, and RCON behavior | P1 | `STC-TI-002`| COMPLETE |`apps/operate/panel/test/server-crud.test.ts`| Strengthened add-server coverage for persisted row/access, RCON probe/connect arguments, encrypted password storage, duplicate server access grant, and failure boundaries. |`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern add-server`; `npx -y -p node@22 -p npm@10 npm run lint`| Complete: tests now fail if add-server returns success without persistence, access grant, encryption under`RCON_SECRET_KEY`, or saved-row RCON connect behavior. | - | The npm test-name pattern is appended after the script's file glob, so the command ran the full suite instead of only add-server tests. | 2026-05-26: Unit suite passed 283 tests; lint passed.  &#124; |
| SRP-005 | Prove delete-server shared access and cleanup outcomes | P1 | `STC-TI-004`, `STC-FL-010`| COMPLETE |`apps/operate/panel/routes/server.ts`; `apps/operate/panel/test/server-crud.test.ts`; `apps/operate/panel/docs/API.md`| Added shared-access, orphan-delete, cleanup-failure, and malformed-ID delete tests. |`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern delete-server`; `npx -y -p node@22 -p npm@10 npm run lint` | Complete: delete responses now state whether the server row was deleted and whether RCON cleanup was completed, failed, or not needed. | - | The npm test-name pattern is appended after the script's file glob, so the command ran the full suite instead of only delete-server tests. | 2026-05-26: Unit suite passed 287 tests; lint passed.  &#124; |
| SRP-006 | Prove game route command sequences and persistence gates | P1 | `STC-TI-005`| COMPLETE |`apps/operate/panel/test/game-routes.test.ts`| Added intent checks for setup-game command order, no-command validation rejects, no persistence after late setup failure, exact workshop-map command, exact sanitized say-admin command, and shared-session reuse to avoid auth limiter noise in the expanded route suite. |`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern setup-game`; `npx -y -p node@22 -p npm@10 npm run lint` | Complete: game-route tests now fail if setup actions send unsafe/out-of-order commands, persist requested state after a late RCON failure, or report workshop/say-admin success without the expected RCON command. | - | The npm test-name pattern is appended after the script's file glob, so the command ran the full suite instead of only setup-game tests. | 2026-05-26: First run exposed auth-rate-limit noise from expanded tests; after reusing a shared session like app-route tests, unit suite passed 289 tests and lint passed.  &#124; |
| SRP-007 | Make unverified game-control success wording truthful | P1 | `STC-FL-003`| COMPLETE |`apps/operate/panel/routes/game/helpers.ts`; `apps/operate/panel/routes/game/controls.ts`; `apps/operate/panel/routes/game/match.ts`; `apps/operate/panel/test/game-routes.test.ts`| Added route coverage proving resolved RCON output that looks error-like still returns non-definitive command-dispatch wording; updated exact setup, backup-restore, and say-admin message expectations. |`npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run test:e2e` | Complete: RCON-backed game controls now say command/command sequence sent instead of claiming restarted, paused, kicked, muted, loaded, restored, enabled, disabled, or created unless that state is actually observed. | - | Message changes are user-visible in API responses and UI toasts; no live CS2/RCON server was available, so verification used mocked RCON and Playwright UI smoke coverage. | 2026-05-26: Unit suite passed 290 tests; lint passed; E2E passed 12 tests.  &#124; |
| SRP-008 | Separate setup-game requested state from observed state | P1 | STC-FL-004| COMPLETE |apps/operate/panel/routes/game/match.ts; apps/operate/panel/routes/server.ts; apps/operate/panel/views/manage.ejs; apps/operate/panel/public/ts/manage.ts; apps/operate/panel/test/game-routes.test.ts; apps/operate/panel/test/e2e/panel.spec.ts; apps/operate/panel/docs/API.md| Added setup-game response fields for requested state, covered error-like resolved RCON output as requested/not observed, renamed manage-page aliases to requested state, and updated E2E/API docs for the requested setup semantics. |npx -y -p node@22 -p npm@10 npm test; npx -y -p node@22 -p npm@10 npm run lint; npx -y -p node@22 -p npm@10 npm run test:e2e; rg -n "Deploy Match|Setup Game failed|lastGameType|lastGameMode|lastMap &#124; data-last-map &#124; Game Created &#124; setup_state &#124; requested_setup &#124; Requested Setup &#124; Send Setup Commands" ... &#124;  Complete: setup-game stores and renders the last requested setup selection, returnssetup_state: "requested"andobserved: false, and no longer presents the manage-page setup form as a verified deployment/applied runtime state.  &#124;  -  &#124;  The physical SQLite columns remain last_* for compatibility; route aliases and API/UI copy define them as requested setup state. No live CS2/RCON readback was available.  &#124;  2026-05-26: Unit suite passed 291 tests; lint passed; E2E passed 12 tests; label scan found only the new requested-state terms and no old setup labels.  &#124; |
| SRP-009 | Make backup route uncertainty explicit | P1 | `STC-FL-005`| COMPLETE |`apps/operate/panel/routes/game/match.ts`; `apps/operate/panel/test/game-routes.test.ts`; `apps/operate/panel/docs/API.md`| Added backup list/latest coverage for listed backups, explicit no-backup responses, empty unknown responses, malformed latest-backup responses, unsafe filenames, and valid restore-request dispatch. |`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern backup`; `npx -y -p node@22 -p npm@10 npm run lint` | Complete: empty, malformed, and unsafe backup responses now return explicit uncertainty states instead of being reported as proof that no backup exists. | - | The npm test-name pattern is appended after the script's file glob, so the command ran the full suite instead of only backup tests. No live CS2/MatchZy backup output was available. | 2026-05-26: Unit suite passed 291 tests; lint passed.  &#124; |
| SRP-010 | Separate raw RCON command success from history persistence | P1 | `STC-FL-006`| COMPLETE |`apps/operate/panel/routes/game/match.ts`; `apps/operate/panel/public/ts/common.ts`; `apps/operate/panel/public/ts/manage.ts`; `apps/operate/panel/test/game-routes.test.ts`; `apps/operate/panel/test/e2e/panel.spec.ts`; `apps/operate/panel/docs/API.md`| Added route coverage for command/history success, history persistence failure after dispatch, and RCON dispatch failure with no history write; added E2E coverage that partial success with output still renders output and shows the history warning. |`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern rcon`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run test:e2e`| Complete:`/api/rcon`now reports`command_sent`, `history_recorded`, and `partial`, so a sent command is not reported as failed only because command-history persistence failed. | - | The npm test-name pattern is appended after the script's file glob; this run compiled the project and ran the full unit suite. History persistence failure was simulated with a SQLite trigger, not a filesystem/database outage. | 2026-05-26: Unit suite passed 293 tests; lint passed; E2E passed 12 tests.  &#124; |
| SRP-011 | Make RCON history wording and UI failure states truthful | P2 | `STC-FL-007`, `STC-FL-008`, `STC-TI-007`| COMPLETE |`apps/operate/panel/routes/operator.ts`; `apps/operate/panel/public/ts/manage.ts`; `apps/operate/panel/views/manage.ejs`; `apps/operate/panel/test/game-routes.test.ts`; `apps/operate/panel/test/e2e/panel.spec.ts`; `apps/operate/panel/docs/API.md`| Updated sent-command wording, route JSON error states, unit history state assertion, and E2E coverage for a failed history fetch rendering unavailable instead of empty. |`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern history`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run test:e2e`| Complete: the UI labels history as sent commands, empty history differs from unavailable history, and API failures return explicit`history_state: "unavailable"`. | - | The npm test-name pattern is appended after the script's file glob; this run compiled the project and ran the full unit suite. UI failure coverage uses Playwright route interception rather than inducing an actual SQLite read failure. | 2026-05-26: Unit suite passed 293 tests; lint passed; E2E passed 13 tests.  &#124; |
| SRP-012 | Classify RCON secret decrypt failures separately from auth failures | P1 | `STC-FL-011`, `STC-TI-016`| COMPLETE |`apps/operate/panel/utils/rconSecret.ts`; `apps/operate/panel/modules/rcon.ts`; `apps/operate/panel/routes/server.ts`; `apps/operate/panel/routes/game/helpers.ts`; `apps/operate/panel/test/rcon-secret.test.ts`; `apps/operate/panel/test/rcon-manager.test.ts`; `apps/operate/panel/docs/API.md`| Added typed decrypt/key/payload errors, malformed/missing/non-hex/wrong-key/tamper tests, and manager tests proving local decrypt failure is reported before any socket/auth attempt while remote auth failure still returns false. |`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern rcon-secret`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run lint` | Complete: malformed or undecryptable stored RCON credentials now surface as local credential storage/decrypt failures instead of generic remote authentication failure. | - | Route-level credential error branches are covered through manager/unit behavior and broad route compilation, not by an end-to-end corrupt stored credential scenario. | 2026-05-26: Focused command passed 298 tests; broad unit suite passed 298 tests; lint passed.  &#124; |
| SRP-013 | Add RCON protocol-level integration fixture | P1 | `STC-TI-011`| COMPLETE |`apps/operate/panel/test/rcon-manager-protocol.test.ts`| Added a local TCP Source RCON protocol fixture that drives real`rcon-srcds`packet auth/command behavior through`RconManager`, covering auth success, auth rejection, delayed auth timeout, command timeout after socket close, and reconnect. | `npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern RconManager`; `npx -y -p node@22 -p npm@10 npm run lint` | Complete: manager tests now cover real packet-level auth and command lifecycle behavior that the class mock cannot exercise. | - | The fixture still mocks DB server discovery and host validation so the test stays local and deterministic; it does not launch a real CS2 server. | 2026-05-26: Unit suite passed 302 tests; lint passed.  &#124; |
| SRP-014 | Cover RCON shutdown, remove, heartbeat, and queue transitions | P1 | `STC-TI-012`, `STC-FL-010`| COMPLETE |`apps/operate/panel/modules/rcon.ts`; `apps/operate/panel/test/rcon-manager.test.ts`| Added lifecycle tests for remove while a command is in flight, repeated remove, shutdown with queued commands, unconfirmed cleanup summaries, heartbeat reconnect failure/backoff, and multi-server isolation. |`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern RconManager`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm test`| Complete: shutdown/remove no longer claim clean closure when disconnect is unconfirmed, and removed server IDs cannot reconnect from queued or in-flight command paths. | - | Timeout cleanup was simulated with a fake socket that never emits`close`; no real RCON server cleanup failure was available. | 2026-05-26: Initial RconManager run exposed an unhandled rejection in the new test harness; after attaching rejection observation immediately, targeted run passed 306 tests, broad unit suite passed 306 tests, and lint passed.  &#124; |
| SRP-015 | Prove updater stop/update/verify/start ordering | P1 | `STC-TI-013`| COMPLETE |`apps/maintain/updater/tests/run.sh`; `apps/maintain/updater/tests/bin/systemctl`; `apps/maintain/updater/tests/bin/steamcmd`| Added a shared ordered event log across updater test stubs, including manifest build-ID reads via a test-only`awk`wrapper, and asserted ordering for update applied, update failed, unchanged build ID, start failure, initially inactive service, dry-run, and unknown remote status. |`make test`; `make lint`in`apps/maintain/updater`| Complete: updater tests now fail if update operations run before service stop, service start runs before post-update build-ID read, or dry-run/unknown-remote paths stop/update/start the service. | - | The build-ID verification event is captured through the test harness`awk` wrapper around manifest reads; it does not instrument production code. | 2026-05-26: Updater test suite passed 49 tests; updater lint passed.  &#124; |
| SRP-016 | Verify updater service is active after start | P1 | `STC-FL-013`, `STC-TI-013`| COMPLETE |`apps/maintain/updater/update_cs2.sh`; `apps/maintain/updater/tests/run.sh`; `apps/maintain/updater/tests/bin/systemctl`| Added bounded post-start`systemctl is-active --quiet`checks, explicit failed-update restart uncertainty, and tests for start success with inactive service, start success with active service, start command failure, and inactive dry-run. |`make test`; `make ci`in`apps/maintain/updater`| Complete: the updater cannot log update/start success unless the service is proven active; if a failed-update restart returns success but remains inactive, the run reports that uncertainty before exiting nonzero. | - | Unit-type behavior still depends on`systemctl is-active` semantics on the target host; tests use a deterministic stub. | 2026-05-26: Updater tests passed 50 tests; updater CI passed lint, tests, and security.  &#124; |
| SRP-017 | Define and enforce updater config unknown/empty-key policy | P2 | `STC-MC-011`, `STC-FL-014`, `STC-TI-014`| COMPLETE |`apps/maintain/updater/update_cs2.sh`; `apps/maintain/updater/tests/run.sh`; `apps/maintain/updater/cs2-auto-update.conf.example`; `apps/maintain/updater/README.md`| Chose and documented the policy: config files accept only documented keys, removed legacy keys warn, duplicate active keys fail, and explicit empty critical config-file values fail. Added tests for unknown keys, removed keys, duplicate keys, quoted values/comments, CLI precedence, explicit empty`SERVICE_NAME`, and non-default service-name side effects. | `rg -n "REMOVED_CONFIG_KEYS|BOGUS_KEY|SERVICE_NAME" apps/maintain/updater`;`make test`;`make ci`in`apps/maintain/updater` | Complete: config typos and empty critical config-file values cannot be mistaken for clean configured success, while existing removed-key compatibility remains observable as a warning. | -  &#124;  Environment variables set to empty still follow the prior defaulting behavior; this slice deliberately scoped empty-value failure to config files because the docs define the config-file policy.  &#124;  2026-05-26: Search confirmed policy/test references; updater tests passed 54 tests; updater CI passed lint, tests, and security.  &#124; |
| SRP-018 | Separate updater test-only knobs from deployable config | P1 | `STC-MC-018`| COMPLETE |`apps/maintain/updater/update_cs2.sh`; `apps/maintain/updater/tests/run.sh`; `apps/maintain/updater/cs2-auto-update.conf.example`; `apps/maintain/updater/README.md`| Removed`ALLOW_NONROOT`and`NO_SLEEP`from the config-file whitelist and deployable example, documented them as environment-only test harness controls, and added coverage that config-file usage fails while env-based test harness usage remains deterministic. |`rg -n "ALLOW_NONROOT|NO_SLEEP" apps/maintain/updater`;`make test`;`make ci`in`apps/maintain/updater`| Complete: test-only controls are no longer presented as supported deployable config, and updater tests still run without relying on deployable config examples. | - | Historical changelog entries still mention prior exposure; active README/config example now define the supported policy. Invalid`ALLOW_NONROOT` env validation is not directly testable in the non-root harness because root gating runs first.  &#124;  2026-05-26: Search confirmed active references are script/test/README and historical changelog only; updater tests passed 55 tests; updater CI passed lint, tests, and security.  &#124; |
| SRP-019 | Prove workshop favorite scope and persistence error classification | P1 | `STC-TI-006`, `STC-FL-012`| COMPLETE |`apps/operate/panel/routes/operator.ts`; `apps/operate/panel/test/game-routes.test.ts`| Added two-user/two-server favorite isolation coverage, scoped update/delete no-mutation checks, duplicate-update conflict coverage, and generic persistence-failure coverage. |`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern favorite`; `npx -y -p node@22 -p npm@10 npm run lint` | Complete: workshop favorites cannot be listed, updated, or deleted outside their owning user/server scope, and update failures distinguish unique conflicts from generic persistence failures. | - | Generic DB failure was simulated with a SQLite trigger, not an actual filesystem or database outage. | 2026-05-26: First focused run exposed an over-broad test assertion that compared timestamp fields outside the scope behavior; after narrowing to id/name/workshop_id, focused command passed 308 tests and lint passed.  &#124; |
| SRP-020 | Add representative CSRF matrix | P2 | `STC-TI-009`| COMPLETE |`apps/operate/panel/test/app.test.ts`| Added a table-driven CSRF matrix for representative server, game, user, operator, auth, POST, PATCH, and DELETE routes plus accepted header/form token and unauthenticated boundary checks. |`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern CSRF`; `npx -y -p node@22 -p npm@10 npm run lint` | Complete: representative state-changing routes now fail if missing/wrong CSRF tokens bypass middleware, valid header/form tokens stop working, or unauthenticated state changes become CSRF failures instead of 401s. | - | This slice did not exhaustively cover every state-changing route; it intentionally uses representative routes from each mounted module. | 2026-05-26: Focused CSRF command passed 310 tests and lint passed.  &#124; |
| SRP-021 | Prove SQLite constraints, indexes, and cascade behavior | P1 | `STC-TI-015`| COMPLETE |`apps/operate/panel/test/migrations.test.ts`| Added fresh-schema and supported-v1-upgrade migration tests for duplicate server IP/port, duplicate workshop favorite/history rows, invalid foreign keys, user/server cascades, foreign-key checks, and explicit query indexes. |`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern migration`; `npx -y -p node@22 -p npm@10 npm run lint` | Complete: migration tests now fail if the current schema loses required uniqueness, foreign-key, cascade, or query-index behavior on fresh databases or supported v1 upgrades. | - | Foreign-key enforcement is enabled in the test connection before checking invalid references and cascades because SQLite enforces foreign keys per connection. | 2026-05-26: First run found a TypeScript helper typing issue; after using an explicit try/catch helper, focused command passed 312 tests and lint passed.  &#124; |
| SRP-022 | Define SQLite migration support window | P2 | `STC-MC-014`| COMPLETE |`apps/operate/panel/test/migrations.test.ts`; `apps/operate/panel/docs/RUNBOOK.md`| Added fixture tests for the supported pre-versioned`user_version=0`inline schema, supported v2 schema, and unsupported future versions; documented the SQLite migration support window and operator backup/boundary rule. |`rg -n "user_version|migration|schema" apps/operate/panel docs`;`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern migration`;`npx -y -p node@22 -p npm@10 npm run lint` | Complete: the repo now defines and tests the supported SQLite startup inputs before any compatibility path is removed. | -  &#124;  No real operator backup database was available; support is proven with code-derived fixtures and child-process DB imports.  &#124;  2026-05-26: Search found the new runbook support-window section and migration fixtures; focused migration command passed 315 tests and lint passed.  &#124; |
| SRP-023 | Remove test-command false certainty | P2 | `STC-TI-010`, `STC-FL-015`, `STC-MC-019`| COMPLETE |`apps/operate/panel/package.json`; `apps/operate/panel/test/game-helpers.test.ts`; `apps/operate/panel/test/mock-module.ts`; `apps/operate/panel/test/runtime.test.ts`| Removed`--test-force-exit`, removed the process-level helper skip, aligned mock-module options to the declared Node 22 engine, and added an explicit runtime test that fails outside the declared engine range. | `node -v`; `npx -y -p node@22 node --experimental-test-module-mocks --test --test-timeout 120000 dist/test/*.test.js`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run lint`| Complete: the test command no longer hides dangling handles with force-exit and unsupported runtimes fail as tests instead of silently skipping helper coverage. | - | Host default`node`is v26.0.0 and remains outside the package engine; verification used Node 22 via`npx`because that matches`package.json`. | 2026-05-26: Pre-change no-force compiled test run exited cleanly with 315 tests; after script/helper changes, `npm test` passed 316 tests and lint passed.  &#124; |
| SRP-024 | Replace source-string tests with behavior or docs-lint checks | P2 | STC-TI-001| COMPLETE |apps/operate/panel/test/scripts.test.ts| Removed redundant docs/template/source-grep assertions, retained behavior coverage in existing auth/CSRF, production Redis fail-fast, and malicious-username E2E tests, and replaced the.gitignorestring check withgit check-ignorebehavior. |rg -n "readFileSync|RateLimitRedisStore|makeRateLimitStore|innerHTML|docs reflect &#124; server route keeps &#124; gitignore" apps/operate/panel/test/scripts.test.ts;npx -y -p node@22 -p npm@10 npm test;npx -y -p node@22 -p npm@10 npm run lint;npx -y -p node@22 -p npm@10 npm run test:e2e &#124;  Complete: the slice no longer has tests that pass solely because implementation strings remain inscripts.test.ts, while the relevant runtime behaviors remain covered.  &#124;  -  &#124;  README/repo-map wording is no longer unit-tested because it was documentation text, not runtime behavior.  &#124;  2026-05-26: Source-grep scan returned no matches; unit suite passed 313 tests; lint passed; E2E passed 13 tests including malicious username DOM rendering.  &#124; |
| SRP-025 | Exercise manage-page controls, not just visibility | P1 | `STC-TI-008`| COMPLETE |`apps/operate/panel/test/e2e/panel.spec.ts`| Added Playwright route-mocked manage-page action coverage for RCON autocomplete selection, send-button disabled/output behavior, history use/clear, workshop favorite edit/launch/delete, and workshop collection validation/error/success outcomes. |`npx -y -p node@22 -p npm@10 npm run test:e2e`; `npx -y -p node@22 -p npm@10 npm run lint`| Complete: high-value manage-page controls are now proven by endpoint payload assertions and visible UI state changes instead of visibility-only checks. | - | The tests assert the browser's current`server_id`payload shape as a string because`getServerId()` is a string page context value. | 2026-05-26: First E2E run exposed a numeric/string payload expectation mismatch in the new test; after aligning to the real runtime payload, E2E passed 18 tests and lint passed.  &#124; |
| SRP-026 | Decide optional operator feature scope | P2 | `STC-MC-001`, `STC-MC-002`, `STC-MC-003`| COMPLETE |`docs/simplicity-remediation-ledger.md`; `docs/simplicity-remediation-status.md`| Investigation only; no runtime tests required because no code or test behavior changed. |`rg -n "autocomplete|rcon_command_history|workshop_favorites|rcon/history|workshop-favorites" .`;`git log --oneline --decorate --all -- apps/operate/panel/routes/operator.ts apps/operate/panel/public/ts/manage.ts apps/operate/panel/views/manage.ejs apps/operate/panel/db.ts apps/operate/panel/docs apps/operate/panel/test`;`gh pr list --state all --limit 20`;`gh issue list --state all --limit 20`;`git log -S ...`searches for the three feature markers.  &#124;  Decision: keep all three features for now, but only as limited current features with no expansion. Persisted history has active API/runbook docs and fail-loud/test coverage; autocomplete and workshop favorites have current route/UI/test coverage but no direct issue/PR/operator-workflow proof. Because evidence is inconclusive rather than a clean unused signal, removal is not planned without explicit operator approval.  &#124;  -  &#124;  Active external operator usage remains UNCLEAR for autocomplete and workshop favorites; no GitHub issues were found, and PR bodies reviewed did not establish feature demand.  &#124;  2026-05-26: Current repo search found code/tests/docs references; GitHub issue list was empty; PR history did not provide direct operator-demand evidence;`git log -S` found no committed history for the current feature markers, so decisions rely on current working-tree evidence and preserve uncertainty.  &#124; |
| SRP-027 | Prove or remove `DEFAULT_PORT`compatibility | P2 |`STC-MC-007`| COMPLETE |`apps/operate/panel/app.ts`; `apps/operate/panel/test/entrypoint.test.ts`| Added entrypoint coverage proving an explicit`PORT`is used and`DEFAULT_PORT`is ignored, plus unset`PORT`defaults to`3000`. | `rg -n "DEFAULT_PORT" .`; `npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern entrypoint`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run lint`| Complete: panel startup now has one bind-port contract,`PORT`with default`3000`; `DEFAULT_PORT`no longer affects runtime behavior. | - | Hidden deployments that set only`DEFAULT_PORT`would now fall back to`3000`; no live deployment telemetry was available. | 2026-05-26: Repo/docs/examples/history search found no active `DEFAULT_PORT` contract outside audits/remediation; unit command passed 315 tests, typecheck passed, and lint passed.  &#124; |
| SRP-028 | Prove or simplify Redis host/port aliases | P2 | `STC-MC-008`| COMPLETE |`apps/operate/panel/utils/redis.ts`; `apps/operate/panel/app.ts`; `apps/operate/panel/.env.example`; `apps/operate/panel/test/entrypoint.test.ts`| Added a production startup regression that rejects`REDIS_HOST`/`REDIS_PORT`without`REDIS_URL`; existing production mocked-Redis test continues to cover REDIS_URL-backed app import. | `rg -n "REDIS_HOST|REDIS_PORT|REDIS_URL" .`;`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern Redis`;`npx -y -p node@22 -p npm@10 npm run typecheck`;`npx -y -p node@22 -p npm@10 npm run lint`| Complete: Redis config is standardized on`REDIS_URL`; host/port aliases were removed from runtime and `.env.example`, and production startup fails closed if only aliases are set. | -  &#124;  Hidden deployments using only `REDIS_HOST`/`REDIS_PORT`must migrate to`REDIS_URL`; no external deployment telemetry was available.  &#124;  2026-05-26: Active docs/examples favored `REDIS_URL`; unit command passed 316 tests, typecheck passed, and lint passed.  &#124; |
| SRP-029 | Prove or constrain full CSP override | P2 | `STC-MC-010`| COMPLETE |`apps/operate/panel/app.ts`; `apps/operate/panel/.env.example`; `apps/operate/panel/test/entrypoint.test.ts`| Added an entrypoint regression that sets`CONTENT_SECURITY_POLICY`but still observes the generated nonce-based CSP header and matching rendered script nonce. |`rg -n "CONTENT_SECURITY_POLICY" .`; `npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern CSP`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm run test:e2e`| Complete: the full CSP override is removed,`.env.example` no longer advertises it, and CSP is always generated with the page nonce. | - | Hidden deployments that depended on replacing CSP must move that policy to a reverse proxy or request an explicit constrained extension. | 2026-05-26: No deployment docs/history proved custom CSP usage; unit command passed 317 tests, typecheck/lint passed, and E2E passed 18 tests.  &#124; |
| SRP-030 | Prove or remove `RCON_AUTH_TIMEOUT_MS`runtime knob | P2 |`STC-MC-009`| COMPLETE |`apps/operate/panel/modules/rcon.ts`; `apps/operate/panel/test/rcon-manager.test.ts`; `apps/operate/panel/test/rcon-manager-protocol.test.ts`| Updated RCON auth-timeout tests to inject a test-only manager option instead of mutating a deployable env var. |`rg -n "RCON_AUTH_TIMEOUT_MS" .`; `git log -S "RCON_AUTH_TIMEOUT_MS" --oneline --decorate --all -- .`; `npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern RconManager`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run lint`| Complete: RCON authentication timeout remains fixed at the production default unless tests pass an internal option; the deployable`RCON_AUTH_TIMEOUT_MS`env knob was removed from runtime/tests. | - | Hidden deployments using`RCON_AUTH_TIMEOUT_MS` would lose that tuning path; no active docs, env examples, or git-history decision proved it was a supported operator contract. | 2026-05-26: Targeted RCON command passed 317 tests; typecheck and lint passed; final search found the env-var string only in audit/remediation/archive docs.  &#124; |
| SRP-031 | Decide whether `scripts/validate.sh`is public compatibility | P3 |`STC-MC-012`| COMPLETE |`README.md`| None added; this slice documented an existing compatibility alias. |`rg -n "scripts/validate.sh|validate.sh --require-docker|./scripts/validate.sh" .`;`git log --all --oneline --decorate --follow -- scripts/validate.sh`;`git log --all --oneline --decorate --follow -- apps/operate/panel/scripts/validate.sh`;`./scripts/verify.sh`| Complete:`./scripts/verify.sh`is the documented canonical root verifier, while root`./scripts/validate.sh` is explicitly retained only as a compatibility alias. | -  &#124;  No external operator telemetry proves whether anyone still calls the root alias; removal remains unjustified. Root verifier is still blocked after shared checks by unavailable Docker daemon access in this environment.  &#124;  2026-05-26: Search separated root and panel validate scripts; history shows the root wrapper came from shared verification workflow history; root verifier passed shared shell/config checks, then failed at Docker daemon access.  &#124; |
| SRP-032 | Decide whether pinned shell-tool downloader is required | P2 | `STC-MC-013`| COMPLETE |`apps/maintain/updater/CONTRIBUTING.md`| None added; this slice documented the existing manual-only tool bootstrap path. |`rg -n "ci-install-tools|ci-tools-versions" .`;`make lint`;`make security`in`apps/maintain/updater` | Complete: the pinned downloader remains a local/manual setup helper for required tool versions with checksum verification; GitHub Actions remains on the simpler apt-installed CI path. | - | No contributor telemetry proves use of the manual downloader; deleting it remains unjustified because active updater docs and lint file lists still include it.  &#124;  2026-05-26: Search found the helper, manifest, lint list, changelog, and docs references; updater lint and security passed.  &#124; |
| SRP-033 | Decide CommonJS deprecation and Node mock-helper contract | P2 | `STC-MC-015`, `STC-MC-019`| COMPLETE |`apps/operate/panel/tsconfig.json`| Existing runtime test already enforces Node 22; no new tests added. |`rg -n "Node 26|>=26|process\\.versions\\.node|mock\\.module|ignoreDeprecations &#124; Current decision &#124; CommonJS" apps/operate/panel/package.json apps/operate/panel/tsconfig.json apps/operate/panel/test`;`npx -y -p node@22 node -v`;`npx -y -p node@22 -p npm@10 npm run build`;`npx -y -p node@22 -p npm@10 npm test`;`npx -y -p node@22 -p npm@10 npm run test:e2e`;`npx -y -p node@22 -p npm@10 npm run lint`  &#124;  Complete: panel build/test/runtime contract is explicitly CommonJS on Node 22, the mock helper has no Node 26 branch, and ESM/NodeNext migration is deferred to a dedicated verified change.  &#124;  -  &#124;  Docker image/startup smoke was not rerun for this slice because Docker daemon access is unavailable; Playwright E2E exercised the built app server locally.  &#124;  2026-05-26: Node 22.22.3 used; build passed; unit suite passed 317 tests; E2E passed 18 tests; lint passed.  &#124; |
| SRP-034 | Reassess RCON password provider and lazy DB seam after RCON tests | P2 | `STC-MC-016`| COMPLETE |`apps/operate/panel/modules/rcon.ts`| Existing RCON manager/protocol tests retained; no new tests added. |`rg -n "new RconManager|PasswordProvider|passwordProvider|fetchPasswordFromDb|better_sqlite_client &#124; require\\('../db'\\) &#124; modules/rcon" apps/operate/panel`;`npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern RconManager`;`npx -y -p node@22 -p npm@10 npm run typecheck`;`npx -y -p node@22 -p npm@10 npm run lint`;`npx -y -p node@22 -p npm@10 npm run build`;`npx -y -p node@22 -p npm@10 npm run test:e2e`;`rg -n "require\\('../db'\\) &#124; fetchPasswordFromDb &#124; new RconManager\\(\\)" apps/operate/panel/modules/rcon.ts apps/operate/panel/test`  &#124;  Complete: RCON now imports SQLite explicitly and the production singleton passes a concrete SQLite password provider; the provider seam is retained for isolated RCON manager/protocol tests.  &#124;  -  &#124;  No live CS2/RCON manual smoke was available; no production alternate password source was found, so the retained provider seam is test-only by current evidence.  &#124;  2026-05-26: Targeted RCON command passed 317 tests; typecheck/lint/build passed; E2E passed 18 tests; final search found no lazy DB require, no old fetch helper name, and no no-arg manager construction.  &#124; |
| SRP-035 | Separate pure server ID parsing from route access side effects | P2 | STC-MC-017| COMPLETE |apps/operate/panel/utils/parseServerId.ts; apps/operate/panel/utils/serverAccess.ts; apps/operate/panel/routes/server.ts; apps/operate/panel/routes/operator.ts; apps/operate/panel/routes/game/controls.ts; apps/operate/panel/routes/game/helpers.ts; apps/operate/panel/routes/game/match.ts| Existing parser, route authorization, and E2E tests retained; no new tests added. |rg -n "parseServerId|requireServerId|requireAuthorizedServerId|requireAuthorizedServerIdParam|server_id" apps/operate/panel/routes apps/operate/panel/test;npx -y -p node@22 -p npm@10 npm run typecheck;npx -y -p node@22 -p npm@10 npm run lint;npx -y -p node@22 -p npm@10 npm test;npx -y -p node@22 -p npm@10 npm run test:e2e;rg -n "requireServerId &#124; requireAuthorizedServerId &#124; requireAuthorizedServerIdParam &#124; Request &#124; Response &#124; better_sqlite_client &#124; require\\('../db'\\)" apps/operate/panel/utils/parseServerId.ts apps/operate/panel/utils/serverAccess.ts &#124;  Complete:parseServerId.tsis now pure ID parsing only, while response-writing access checks live inserverAccess.tsand route behavior remains covered.  &#124;  -  &#124;  No behavior change intended; route access coverage is existing representative coverage rather than a new exhaustive matrix for every endpoint.  &#124;  2026-05-26: Typecheck/lint passed; unit suite passed 317 tests; E2E passed 18 tests; final search confirmed Express/SQLite access code is no longer inparseServerId.ts.  &#124; |
| SRP-036 | Inline `makeMultiPresetRoute`| P2 |`STC-MC-004`| COMPLETE |`apps/operate/panel/routes/game/controls.ts`; `apps/operate/panel/routes/game/helpers.ts`| Existing game-route sequence and invalid-value tests retained; no new tests added. |`rg -n "makeMultiPresetRoute" apps/operate/panel`; `npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern set-startmoney`; `npx -y -p node@22 -p npm@10 npm test -- --test-name-pattern set-roundtime`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run lint`| Complete: the two multi-command preset routes are direct handlers and the generic`makeMultiPresetRoute`helper is removed. | - | No live CS2/RCON smoke was available; route behavior is covered by mocked RCON tests. The npm test-name pattern still runs the full compiled unit suite. | 2026-05-26: Final search found no`makeMultiPresetRoute` references; both targeted commands passed 317 tests; typecheck and lint passed.  &#124; |
| SRP-037 | Inline browser server ID context | P2 | STC-MC-005| COMPLETE |apps/operate/panel/public/ts/console.ts; apps/operate/panel/public/ts/manage.ts; apps/operate/panel/public/ts/context.ts| Existing manage-page E2E payload tests retained; no new tests added. |rg -n "context|setServerId|getServerId|from './context'" apps/operate/panel/public/ts apps/operate/panel/views apps/operate/panel/test;rg --files apps/operate/panel/public/ts &#124; rg "context\\.ts";npx -y -p node@22 -p npm@10 npm run build:client;npx -y -p node@22 -p npm@10 npm run typecheck;npx -y -p node@22 -p npm@10 npm run lint;npx -y -p node@22 -p npm@10 npm run test:e2e &#124;  Complete:console.tsreads the manage-page server ID once and passes it intoinitManagePage; manage.tsthreads that value through server-scoped handlers; the mutablecontext.tsmodule was deleted.  &#124;  -  &#124;  Missingdata-server-id would still pass an empty string into handlers, matching the previous runtime fallback; no separate missing-DOM-ID failure behavior was added in this simplification slice.  &#124;  2026-05-26: Reference scans found no context/getServerId usage and no remaining context file; client build, typecheck, lint, and E2E passed.  &#124; | |
| SRP-038 | Deduplicate browser JSON request helpers | P2 | `STC-MC-006`| COMPLETE |`apps/operate/panel/public/ts/common.ts`; `apps/operate/panel/public/ts/manage.ts`; `apps/operate/panel/public/ts/servers.ts`; `apps/operate/panel/test/e2e/panel.spec.ts`| Updated workshop collection E2E coverage to prove shared JSON error handling for non-JSON failures, JSON error messages, and success clearing. | Duplicate-helper`rg`scan;`npx -y -p node@22 -p npm@10 npm run build:client`; `npx -y -p node@22 -p npm@10 npm run typecheck`; `npx -y -p node@22 -p npm@10 npm run lint`; `npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run test:e2e`| Complete: browser JSON request handling is centralized in`common.ts`; `sendPostRequest`remains only as a thin POST wrapper; manage/server browser code no longer defines local JSON/CSRF/request helpers. | - | No exhaustive route-intercept matrix was added for every browser request; representative E2E coverage and full unit/E2E suites passed. | 2026-05-26: Final search found only`common.ts`defining`csrfHeaders`/`fetchJson`, with manage/server pages importing the helper; client build, typecheck, lint, unit suite, and E2E all passed.  &#124; |
| SRP-039 | Rename and split utility tests around intent | P3 | `STC-TI-017`| COMPLETE |`apps/operate/panel/test/game-helpers.test.ts`; `apps/operate/panel/test/parse-server-id.test.ts`; `apps/operate/panel/test/rcon-response.test.ts`; `apps/operate/panel/test/game-routes.test.ts`| Grouped utility examples under policy-focused test names and split bundled setup/player/backup route tests into separate intent-named cases. |`rg`scan for cited weak names;`npx -y -p node@22 -p npm@10 npm test`; `npx -y -p node@22 -p npm@10 npm run lint` | Complete: the same edge examples are covered under names that state canonical ID parsing, safe RCON command text, display sanitization, setup failure boundaries, player action boundaries, and backup state classification. | - | Test count changed because utility one-liners were grouped; no production behavior changed. | 2026-05-26: Weak-name scan found no cited names or bundled command-boundary route test; unit command passed 284 tests and lint passed.  &#124; |
