name: verify
on:
  push:
    branches: [dev]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: git status --short
docker: bootstrap
github-actions: core-build-out
add-shared-verification-: expansion
python: publication
