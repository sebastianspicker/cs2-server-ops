export function createReferenceSummary() {
  return { scope: "reference", status: "ready" };
}

// current lane: reference
export function referenceTask() {
  return { scope: "reference", status: "ready" };
}
